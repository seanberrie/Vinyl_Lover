<div class="addback">
<div class= "container">
<h1 class="word"> My Records </h1>
<h2 class="word">Own</h2>
<div class="list">
<% @user.records.each do |r| %>
    <% if r.own === true %>
    <div class="newrec">
            <% if r.image != "" %> <img class="img" src="<%= r.image %>"> <% end %>
            <p><%= r.artist %> 
            <p><%= r.album %></p>
            <p><%= r.year %></p>
            <%= link_to "edit", edit_user_record_path(@user.id, r.id), class: 'btn btn-primary'%>
            </div>
            <% end %>
            <% end %>
</div>
 
<h2 class="word">Want</h2>
    <div class="list">
    <% @user.records.each do |r| %>
        <% if r.own === false %>
        <div class="newrec">
            <% if r.image != "" %> <img class="img" src="<%= r.image %>"> <% end %>
            <p><%= r.artist %> 
            <p><%= r.album %></p>
            <p><%= r.year %></p>
            <%= link_to "edit", edit_user_record_path(@user.id, r.id),class: 'btn btn-primary' %>
            </div>
            <% end %>
        <% end %>
    </div>    
</div>

